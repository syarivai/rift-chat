# Copy to .env. Values prefixed EXPO_PUBLIC_ are inlined into the JS bundle at build time —
# they are readable by anyone who installs the app. Never put a secret here.
EXPO_PUBLIC_API_BASE_URL=https://responserift.dev
